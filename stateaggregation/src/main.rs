use rand::{
    distributions::{Distribution, Standard},
    random, Rng,
};

struct RandomWalkEnvironment {
    position: isize,
    start: isize,
    left_terminal: isize,
    right_terminal: isize,
    num_states: isize,
}

impl RandomWalkEnvironment {}

fn main() {
    println!("Hello, world!");
}
